alias honeygitpn="git syb staging && git pn && hub browse"

alias updateall="brew update && brew upgrade && brew cleanup -s && brew cask upgrade && brew doctor && brew missing && gcloud components update --quiet"
