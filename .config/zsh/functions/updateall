#!/bin/zsh

function updateall() {
  if command -v brew &> /dev/null; then
    brew update
    brew upgrade
    brew cleanup -s
    brew cask upgrade
    brew doctor
    brew missing
  fi

  if command -v gcloud &> /dev/null; then
    gcloud components update --quiet
  fi
}
